/*
function read_file(filename){
  let contents = [];
  var file = new File(["foo"], filename, {
      type: "text/plain",
  });
  console.log(file);
  return file;
}

function json_to_string(json_file){
  let obj = JSON.parse(json_file);
  return obj
}

function string_to_json(string){

  return json_obj
}
*/

// process.env.MONGO_URI = Null;
